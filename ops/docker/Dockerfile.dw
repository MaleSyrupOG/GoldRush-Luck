# syntax=docker/dockerfile:1.7
# Hardened multi-stage Dockerfile for the DeathRoll Deposit/Withdraw bot.
# - Stage 1: builder. Installs uv, syncs the locked dependency set into a venv.
# - Stage 2: runtime. Minimal slim base, non-root user UID 1002, tini as PID 1,
#   no build tooling, read-only filesystem friendly.
#
# Required Python version is pinned via .python-version (3.12). The base image
# matches that pin so wheels resolve cleanly on glibc-based Linux.

# ---------- Stage 1: builder ----------
FROM python:3.12-slim-bookworm AS builder

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1 \
    UV_COMPILE_BYTECODE=1 \
    UV_LINK_MODE=copy

# Install uv from the official static binary (no system Python tooling needed)
RUN apt-get update \
    && apt-get install -y --no-install-recommends curl ca-certificates \
    && curl -LsSf https://astral.sh/uv/install.sh | env UV_INSTALL_DIR=/usr/local/bin sh \
    && apt-get purge -y curl \
    && apt-get autoremove -y \
    && rm -rf /var/lib/apt/lists/*

# Build the venv at the FINAL runtime path /app/.venv from the start so
# script shebangs in the venv (alembic, dotenv, …) reference the correct
# python interpreter location after we copy them to the runtime stage.
WORKDIR /app
COPY pyproject.toml uv.lock .python-version ./
COPY deathroll_core ./deathroll_core
COPY deathroll_luck ./deathroll_luck
COPY deathroll_deposit_withdraw ./deathroll_deposit_withdraw
COPY deathroll_poker ./deathroll_poker
# Alembic migrations are run from inside the bot container during deploy.
COPY ops/alembic ./ops/alembic

# Resolve and install only runtime dependencies (no dev group).
RUN uv sync --frozen --no-dev

# ---------- Stage 2: runtime ----------
FROM python:3.12-slim-bookworm AS runtime

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PYTHONHASHSEED=random \
    PATH="/app/.venv/bin:${PATH}"

# Minimal runtime deps: tini for proper PID 1 + ca-certificates for TLS to Discord/Postgres.
RUN apt-get update \
    && apt-get install -y --no-install-recommends tini ca-certificates \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

# Non-root user with FIXED UID 1002 (Luck uses 1001).
RUN groupadd --system --gid 1002 dwbot \
    && useradd --system --uid 1002 --gid dwbot --shell /usr/sbin/nologin --home /app dwbot

WORKDIR /app

# Copy the venv and the application code from the builder. The builder
# already used /app as its workdir so script shebangs are correct.
COPY --from=builder --chown=dwbot:dwbot /app/.venv /app/.venv
COPY --from=builder --chown=dwbot:dwbot /app/deathroll_core /app/deathroll_core
COPY --from=builder --chown=dwbot:dwbot /app/deathroll_deposit_withdraw /app/deathroll_deposit_withdraw
COPY --from=builder --chown=dwbot:dwbot /app/ops/alembic /app/ops/alembic

USER dwbot

# Default healthcheck: exits 0 if the package is importable and the placeholder
# healthcheck script returns 0. Real DB-aware healthcheck arrives in Epic 4.
HEALTHCHECK --interval=30s --timeout=10s --start-period=30s --retries=3 \
    CMD python -m deathroll_deposit_withdraw.healthcheck || exit 1

ENTRYPOINT ["/usr/bin/tini", "--"]
CMD ["python", "-m", "deathroll_deposit_withdraw"]
