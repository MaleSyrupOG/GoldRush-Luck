# Deposit / Withdraw flow (sequence diagrams)

> **Status**: stub outline (Story 1.5). Cross-references the now-complete D/W docs.

## User-facing walkthroughs

The user-facing walkthroughs already exist in production-quality form:

- `docs/tickets/deposit-flow.md` — `/deposit` end-to-end with worked example.
- `docs/tickets/withdraw-flow.md` — `/withdraw` symmetric flow with the create-time fee capture.
- `docs/tickets/cashier-onboarding.md` — cashier-side flow.
- `docs/tickets/ticket-lifecycle.md` — formal state machine.

## Sequence diagram (planned)

TODO: Story 13.x — produce a mermaid sequence diagram for each major flow. The text in `tickets/` is currently the canonical reference.

## References

- D/W design spec §5
- ADR 0011, 0013, 0017
- `docs/tickets/*`
