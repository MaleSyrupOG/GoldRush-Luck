"""Helpers shared between every Discord cog in the GoldRush bots.

Submodules:

- ``channel_binding``  — read configured channel ids from
                          ``dw.global_config`` and the
                          ``@require_channel`` decorator.
"""

from __future__ import annotations
