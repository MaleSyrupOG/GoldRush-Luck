# DeathRoll Poker

Reserved namespace for the future poker bot. This package is intentionally empty in v1.

When work begins, follow the same brainstorm → spec → plan flow used for `deathroll_luck` and `deathroll_deposit_withdraw`. Specs go to `docs/superpowers/specs/`, ADRs to `docs/adr/`.
