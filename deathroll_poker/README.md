# GoldRush Poker

Reserved namespace for the future poker bot. This package is intentionally empty in v1.

When work begins, follow the same brainstorm → spec → plan flow used for `goldrush_luck` and `goldrush_deposit_withdraw`. Specs go to `docs/superpowers/specs/`, ADRs to `docs/adr/`.
